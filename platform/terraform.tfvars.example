kube_context = "do-nyc1-systems"

# Infrastructure preparation only. Existing apps cannot use PostgreSQL yet.
apps                        = {}
postgres_migration_verified = false

# After app migration, secret bootstrap, image build/push and validation:
# apps = {
#   health = { image = "ghcr.io/freddierice/health@sha256:<64-hex-digest>" }
#   trends = { image = "ghcr.io/freddierice/trends@sha256:<64-hex-digest>" }
# }
# postgres_migration_verified = true
# image_pull_secrets = ["ghcr-pull"]
