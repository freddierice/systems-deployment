apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: systems-caddy-data
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: {{ .Values.gateway.storageClass }}
  resources:
    requests:
      storage: {{ .Values.gateway.storage }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: systems-gateway
spec:
  replicas: 1
  # A single Caddy owns certificate state on a ReadWriteOnce volume.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: systems-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: systems-gateway
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile: {type: RuntimeDefault}
      dnsPolicy: None
      dnsConfig:
        nameservers: [{{ .Values.dns.serviceIP | quote }}]
        searches: [{{ printf "%s.svc.cluster.local" .Release.Namespace | quote }}, svc.cluster.local, cluster.local]
        options: [{name: ndots, value: "1"}]
      containers:
        - name: caddy
          image: {{ .Values.gateway.image | quote }}
          args: [run, --config, /etc/systems/Caddyfile, --adapter, caddyfile]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          ports:
            - {name: http, containerPort: 8080}
            - {name: https, containerPort: 8443}
            - {name: health, containerPort: 8081}
          readinessProbe:
            httpGet: {path: /healthz, port: health}
          livenessProbe:
            httpGet: {path: /healthz, port: health}
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {memory: 256Mi}
          volumeMounts:
            - {name: config, mountPath: /etc/systems, readOnly: true}
            - {name: data, mountPath: /data}
            - {name: caddy-config, mountPath: /config}
            - {name: tmp, mountPath: /tmp}
      volumes:
        - name: config
          configMap: {name: systems-config}
        - name: data
          persistentVolumeClaim: {claimName: systems-caddy-data}
        - name: caddy-config
          emptyDir: {}
        - name: tmp
          emptyDir: {}
